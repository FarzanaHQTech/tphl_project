documents 

// fixed size width and height 
$photo = uploadImage('employeePhoto', 'employees', 'fixed', 300, 300);
// original sizeof()
$photo = uploadImage('employeePhoto', 'employees', 'original');


// controller e arekta folder niye pore controller file likhle index.php te  route differently likhte hobe 

"controller" => "Construction\\ProjectController"